{
  "name": "TradingBot",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/Wscheeee/TradingBot.git",
  "author": "Georgemburu <snooppythedj@gmail.com>",
  "license": "MIT",
  "scripts": {
    "Trader:install": "cd Trader && npm install",
    "MongoDatabase:install": "cd MongoDatabase && npm install",
    "Binance_Scraper:install": "cd Binance_Scraper && npm install",
    "Telegram:install": "cd Telegram && npm install",
    "TelegramChannelsSignalsProxy:install": "cd TelegramChannelsSignalsProxy && npm install",
    "dep-install-npm": "npm install && npm run Trader:install && npm run MongoDatabase:install && npm run Binance_Scraper:install && npm run Telegram:install && npm run TelegramChannelsSignalsProxy:install",
    "dep-install-yarn": "yarn install && yarn Trader:install && yarn MongoDatabase:install && yarn Binance_Scraper:install && yarn Telegram:install && yarn TelegramChannelsSignalsProxy:install"
  },
  "devDependencies": {
    "eslint": "^8.37.0"
  },
  "dependencies": {
    "luxon": "^3.3.0"
  }
}
